### Step1
- 何も見ずに解く
  - 配列numsのサブ配列のそれぞれがkの値と等しいような配列の総数を求める問題
    - サブ配列は、配列内の連続的な要素からなる
  - 最も単純な解き方は、先頭の要素からkの値になるまで加算(calculator)していき、kと等しくなった時点で、calculatorを0に戻す
  - 次の要素から再びkの値になるまで加算
  - 繰り返して末尾まで達したら、サブ配列の総数を返却
