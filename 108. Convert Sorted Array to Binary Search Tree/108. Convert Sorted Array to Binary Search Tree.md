### STEP1
- 何も見ずに解いてみる
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def sortedArrayToBST(self, nums: List[int]) -> Optional[TreeNode]:
        # 二分木なので、常に左の子が右の子より小さい
        # 親は左の子より大きく、右の子より小さい
        # つまり、リストの中央の値が根にあたる
        # 例のOutputを見るとnullが存在する
        # nullとはどのような状況か
        # 数字の構成的にバランスをとる必要がない状況
        # バランスをとる必要がない状況とはどのような状況か
        # 配列が奇数で与えられた状況
        # まずは、配列の中央の値(あるいは中央-1の値)を使ってNodeのvalを設定する
        len_nums = len(nums)
        if len_nums % 2 == 0:
            mid_idx = len_nums / 2
        else:
            mid_idx = int((len_nums - 1) / 2)
            
        mid_num = nums[mid_idx-1]
        root = TreeNode()
        root.val = mid_num
        base_int = -2
        # ノード(根)は左の子より大きい
        root.left = nums[mid_idx+base_int]
        # ノード(根)は右の子より小さい
        base_int += 4
        root.right = nums[mid_idx+base_int]
        # もし、nums[mid_idx-3]がIndexErrorにならなければ、TreeNodeを追加する
        try:
            base_int -= 5
            num = nums[mid_idx+base_int]
            node_left = TreeNode()
            node_left.val = num
            base_int += 3
            node_left.left = nums[mid_idx+base_int]
            base_int += 2
            node_left.right = nums[mid_idx+base_int]
            # root.leftをnodeで上書き
            root.left = node_left

            base_int += 2
            num = nums[mid_idx+base_int]
            node_right = TreeNode()
            node_right.val=num
            base_int += 1
            node_right.left = nums[mid_idx+base_int]
            base_int += 2
            node_right.right = nums[mid_idx+base_int]
            root.right = node_right
        except IndexError:
            pass
        print(root)
```
- そもそもprint(root)ができない
  - エラー内容
  ```
  AttributeError: 'int' object has no attribute 'left'
  Line 76 in _has_cycle (./python3/treenode.py)
  Line 76 in _has_cycle (./python3/treenode.py)
  Line 76 in _has_cycle (./python3/treenode.py)
  Line 85 in has_cycle (./python3/treenode.py)
  Line 16 in __repr__ (./python3/treenode.py)
  Line 13 in __str__ (./python3/treenode.py)
     print(root)
  Line 57 in sortedArrayToBST (Solution.py)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
      ret = Solution().sortedArrayToBST(param_1)
  Line 85 in _driver (Solution.py)
      _driver()
  Line 100 in <module> (Solution.py)
  ```
  - つまり、TreeNodeに正確に値を代入できていないということ
  - 'int' object has no attribute 'left'だから、数値(設定した数値)に対して、leftを(TreeNodeとしての要素とみなして)設定してしまっている
  - 本来、'left'にはintが入るはずだが、TreeNodeが設定されてしまっていることが原因
  - つまり、単純に'TreeNode'でなく、'int'を入れればよい
  - ・・・と思ったが、コメントアウトされていたTreeNodeクラスをコメントアウト外せば通りはしそう
### STEP2
- 他の人の回答を確認する
  ```python
  class Solution:
    def sortedArrayToBST(self, nums: List[int]) -> Optional[TreeNode]:
        def build_tree(left: int, right: int) -> Optional[TreeNode]:
            if left > right:
                return None

            mid = (left + right) // 2
            root = TreeNode(nums[mid])

            root.left = build_tree(left, mid - 1)
            root.right = build_tree(mid + 1, right)

            return root

        return build_tree(0, len(nums) - 1)
  ```
  - 関数の中に関数を作り、再帰的に内部関数を呼び出す
  - それぞれのleft, rightにはintではなく、TreeNodeを入れる
  - 自分なりの回答を考えてみたものの、時間がかかりそうなのでやめた

### STEP3
- 間違えずに3回解く
  - 1回目: 約2分
  - 2回目: 約3分
  - 3回目: 約2分
